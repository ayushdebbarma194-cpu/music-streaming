# ArchiveTune Backend — core runtime dependencies
# This is everything needed for search, playback, downloads, lyrics, scrobbling,
# translation, and together mode. The optional AI provider SDKs live in
# requirements-ai.txt (the code imports them lazily, so they're not required
# unless you use the AI curation / lyric-translation endpoints).

# Core framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic-settings==2.7.1
pydantic==2.10.4

# Async HTTP client
httpx==0.28.1

# WebSocket support (included with uvicorn[standard])
websockets==14.1

# Database
aiosqlite==0.20.0

# YouTube Music API wrapper
ytmusicapi==1.9.1

# Audio playback (requires libmpv on host)
python-mpv==1.0.7

# Translation
deep-translator==1.11.4

# Environment
python-dotenv==1.0.1
